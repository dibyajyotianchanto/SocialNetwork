<%= form_with(model: commentary) do |form| %>
  <% if commentary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commentary.errors.count, "error") %> prohibited this commentary from being saved:</h2>

      <ul>
        <% commentary.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field" hidden>
    <%= form.label :user_id %>
    <%= form.number_field :user_id, value:current_user.id %>
  </div>

  <div class="field" hidden>
    <%= form.label :post_id %>
    <%= form.number_field :post_id, value:@post_id%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
